{% extends 'base.html.twig' %}

{% block title %}MemberType{% endblock %}

{% block body %}
    <h1>MemberType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ member_type.name }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ member_type.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('member_type_index') }}">back to list</a>

    <a href="{{ path('member_type_edit', {'id': member_type.id}) }}">edit</a>

    {{ include('member_type/_delete_form.html.twig') }}
{% endblock %}