{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ category.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ category.lastName }}</td>
            </tr>
            <tr>
                <th>Biography</th>
                <td>{{ category.biography }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ category.birthday ? category.birthday|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('author_index') }}">back to list</a>

    <a href="{{ path('author_edit', {'id': category.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}